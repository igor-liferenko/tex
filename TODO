use absolute paths for TeX* directories, and see default values of format_default_length and file_name_size and see what name_length is used for
see also mytex/TODO
(search format_area_length and use debugger)
-------
see diff -u web2w/tex.w tex.w and try to reduce it
--------
ensure that only file names are converted with mbtowc and remove reverse check (it will be clear if mbtowc() is buggy if there will be warning that file cannot be opened)
