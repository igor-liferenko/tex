ensure that only file names are converted with mbtowc and remove reverse check (it will be clear if mbtowc() is buggy if there will be warning that file cannot be opened)
--------
Move tex repo to web2c repo and this repo to tex repo; afterwards change path.ch and edit.ch and bin/tex and .bashrc and post-install.sh and installer.tex accordingly.
--------
see my_name in tex-sparc/initex.ch and use separate executable for lhplain format in bin/tex
