Move tex repo to web2c repo and this repo to tex repo (with git history); afterwards change path.ch and edit.ch and bin/tex and .bashrc and post-install.sh and installer.tex accordingly.
--------
see ext.w in web2c-tex repo and tex-sparc/ for 'sigaction'
--------
see tex-sparc/ repo for system time
