ensure that only file names are converted with mbtowc and remove reverse check (it will be clear if mbtowc() is buggy if there will be warning that file cannot be opened)
--------
Move tex repo to web2c repo and this repo to tex repo; afterwards change tex.ch and post-install.sh and .bashrc and /usr/local/bin/parse-first-line and bin/tex accordingly.
--------
see my_name in tex-sparc/initex.ch and add -format= option as in mytex/ and change bin/tex and /usr/local/bin/parse-first-line and post-install.sh
