To get UTF-8 changes WRT ctex.w, do

    make -C web2w
    diff tex.w web2w/tex.w

To get changes to web2w.w (to produce modified tex.w), diff web2w/web2w.w with web2w.w from CTAN
(ctex.patch is the same as original); the same for ctangle.patch and common.patch.
    
