Move tex repo to web2c repo and do the following steps again and on each step
review and create diffs and initialize tex repo with the diffs and tex.w and ctex.patch
in the state before last item below.

1:

    make -C web2w
    mv web2w/tex.w .
2:

    perl -i -pe 'next if /^ $/;next if /^ @ $/;s/ +$//' ctex.patch
    perl -i -pe 'next if /^@ $/;s/ +$//' tex.w

3:

    perl -i -pe 's/^(xchr\[\d+\]= )\x27/$1L\x27/' tex.w

4:

change tex.w and ctex.patch to add UTF-8 support
